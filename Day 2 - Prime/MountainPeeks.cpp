#include <bits/stdc++.h>

using namespace std;

int main () {
    int n;
    int count = 0;
    cin >> n;
    int array[n];

    for (int i = 0; i < n; i++) {
        cin >> array[i];
    }

    for (int i = 1; i <= n; i++) {
    }
}